workdir = "deployment_artifacts"
project_home = "https://github.com/data-capsule/psl-cvm"

[deployment_config]
mode = "lan-tiny"
ssh_key = "cluster_key.pem"
ssh_user = "pftadmin"
node_port_base = 3000


[node_config]

[node_config.net_config]
client_max_retry = 10

[node_config.rpc_config]
recv_buffer_size = 32768
channel_depth = 1000

[node_config.consensus_config]
commit_index_gap_soft = 250
commit_index_gap_hard = 5000
liveness_u = 2
max_backlog_batch_size = 1000
signature_max_delay_blocks = 10
signature_max_delay_ms = 10000
num_crypto_workers = 5
view_timeout_ms = 4000
batch_max_delay_ms = 2
max_audit_snapshots = 5

[node_config.consensus_config.log_storage_config.RocksDB]
write_buffer_size = 2147483648
max_write_buffer_number = 1
max_write_buffers_to_merge = 1

[node_config.app_config]
logger_stats_report_ms = 100
checkpoint_interval_ms = 1000

[node_config.evil_config]
simulate_byzantine_behavior = false
byzantine_start_block = 0


[client_config]
full_duplex = true
client_sub_id = 0       # This is filled up later by the caller code.


[client_config.net_config]
client_max_retry = 10

[client_config.workload_config]
max_concurrent_requests = 1
rate = 100.0 # 100 requests per second input rate.


[client_config.workload_config.request_config.AEBlanks]
payload_size = 1_048_576 # 1 MiB



[[experiments]]
name = "storage_sig_1"
type = "psl_storage"
repeats = 1
num_nodes = 1 # This is fixed for this experiment.
node_distribution = "uniform"
build_command = "make"
duration = 60

[experiments.client_config.workload_config.request_config.AEBlanks]
signature_interval = 1

[experiments.sweeping_parameters]
num_clients = [1, 2, 5, 10, 20, 50, 100, 200]


[[experiments]]
name = "storage_sig_2"
type = "psl_storage"
repeats = 1
num_nodes = 1 # This is fixed for this experiment.
node_distribution = "uniform"
build_command = "make"
duration = 60

[experiments.client_config.workload_config.request_config.AEBlanks]
signature_interval = 2

[experiments.sweeping_parameters]
num_clients = [1, 2, 5, 10, 20, 50, 100, 200]



[[experiments]]
name = "storage_sig_10"
type = "psl_storage"
repeats = 1
num_nodes = 1 # This is fixed for this experiment.
node_distribution = "uniform"
build_command = "make"
duration = 60

[experiments.client_config.workload_config.request_config.AEBlanks]
signature_interval = 10

[experiments.sweeping_parameters]
num_clients = [1, 2, 5, 10, 20, 50, 100, 200]

[[experiments]]
name = "storage_sig_20"
type = "psl_storage"
repeats = 1
num_nodes = 1 # This is fixed for this experiment.
node_distribution = "uniform"
build_command = "make"
duration = 60

[experiments.client_config.workload_config.request_config.AEBlanks]
signature_interval = 20

[experiments.sweeping_parameters]
num_clients = [1, 2, 5, 10, 20, 50, 100, 200]


[[experiments]]
name = "storage_sig_inf"
type = "psl_storage"
repeats = 1
num_nodes = 1 # This is fixed for this experiment.
node_distribution = "uniform"
build_command = "make"
duration = 60

[experiments.client_config.workload_config.request_config.AEBlanks]
signature_interval = 1000000000

[experiments.sweeping_parameters]
num_clients = [1, 2, 5, 10, 20, 50, 100, 200]



[[results]]
name = "throughput_client_sweep"
plotter = "throughput_client_sweep"
ramp_up = 0
ramp_down = 0
output = "storage.pdf"
force_parse = true

# input_rate_slope = 100.0 # 100 rps x 1 MiB = 100 MiB/s


[results.legends]
storage_sig_1 = "sig\\_interval=1"
storage_sig_2 = "sig\\_interval=2"
storage_sig_10 = "sig\\_interval=10"
storage_sig_20 = "sig\\_interval=20"
storage_sig_inf = "sig\\_interval=$\\infty$"